DEFAULT_STATE = {
    vars = {
        kind = 'shark_share_counter',
    },
    tables = {
        share_counter = {
            company = 'yellow',
        },
    },
}

function onLoad(save_state)
    Global.call('initObjectState', { object = self, state = DEFAULT_STATE })
end
